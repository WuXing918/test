对于单例模式来说应该注意的几点如下：
1. 单例模式主要是指 该类只有一个实例，其余的都是指针或者引用，当这个仅有的实例被销毁后，可能会出现，指针为空的现象，或者引用悬空的情况。
2. 对于多线程来说，	其核心部分 New Singleton; 必须采用互斥锁，防止两个或者多个线程同时进行创建实例对象。


对于单例模式的理解：
	1, 静态函数不能访问非静态成员（成员函数，成员变量）
	2, 单例模式中是有指针的,所以自己是必须实现拷贝构造函数，和赋值重载函数的。但是在有些类中我们是不会使用到这两个函数的，所以我们可以只进行声明而不进行实现.
